This template creates AWS DynamoDB</a> table.

### Output

* Resources created:
    * 1 example *Individual_Score* DynamoDB table with a primary partition key *name* (type string), and a primary sort key *score* (type string).